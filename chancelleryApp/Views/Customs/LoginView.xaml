<UserControl x:Class="chancelleryApp.Views.Customs.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:chancelleryApp.Views.Customs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock>Логин:</TextBlock>
            <TextBox Text="{Binding Login}"></TextBox>
            <TextBlock>Пароль:</TextBlock>
            <TextBox Text="{Binding Password}"></TextBox>
            <Button Content="Авторизоваться" Command="{Binding LoginCommand}" Margin="0 10 0 0"></Button>
        </StackPanel>
    </Grid>
</UserControl>
